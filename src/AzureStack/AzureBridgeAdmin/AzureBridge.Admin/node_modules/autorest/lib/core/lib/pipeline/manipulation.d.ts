import { DataHandleRead, DataStoreView } from "../data-store/data-store";
import { ConfigurationView } from "../autorest-core";
export declare class Manipulator {
    private config;
    private transformations;
    private ctr;
    constructor(config: ConfigurationView);
    private MatchesSourceFilter(document, transform);
    private ProcessInternal(data, scope, documentId?);
    Process(data: DataHandleRead, scope: DataStoreView, documentId?: string): Promise<DataHandleRead>;
}
