import { ConfigurationView } from "../configuration";
import { DataStoreView, DataStoreViewReadonly } from "../data-store/data-store";
import { IFileSystem } from "../file-system";
export declare type PipelinePlugin = (config: ConfigurationView, input: DataStoreViewReadonly, working: DataStoreView, output: DataStoreView) => Promise<void>;
export declare function RunPipeline(configView: ConfigurationView, fileSystem: IFileSystem): Promise<void>;
