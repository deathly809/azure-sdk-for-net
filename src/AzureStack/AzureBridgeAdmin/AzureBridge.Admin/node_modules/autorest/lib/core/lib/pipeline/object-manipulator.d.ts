import { DataHandleRead, DataHandleWrite } from "../data-store/data-store";
import { JsonPath } from "../ref/jsonpath";
import { SmartPosition } from "../ref/source-map";
export declare function ManipulateObject(src: DataHandleRead, target: DataHandleWrite, whereJsonQuery: string, transformer: (doc: any, obj: any, path: JsonPath) => any, mappingInfo?: {
    transformerSourceHandle: DataHandleRead;
    transformerSourcePosition: SmartPosition;
    reason: string;
}): Promise<{
    anyHit: boolean;
    result: DataHandleRead;
}>;
